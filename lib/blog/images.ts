export function getBlogSummaryImage(slug: string): string {
  return `/images/blog/${slug}-summary.webp`;
}

export function getBlogDetailImage(slug: string): string {
  return `/images/blog/${slug}-detail.webp`;
}
